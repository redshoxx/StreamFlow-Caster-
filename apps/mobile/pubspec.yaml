name: streamflow
description: StreamFlow mobile browser and cast controller.
publish_to: "none"
version: 0.4.0+4

environment:
  sdk: ">=3.12.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.4.2
  webview_flutter: ^4.14.1
  bonsoir: ^7.1.4
  http: ^1.5.0
  file_selector: ^1.1.0
  shared_preferences: ^2.5.5
  path: ^1.9.1
  mime: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
